const { List } = require('./list.model');
const { Task } = require('./task.model');
const { User } = require('./user.model');
const { ActionReceipt } = require('./action-receipt.model');


module.exports = {
    List,
    Task,
    User,
    ActionReceipt
}
